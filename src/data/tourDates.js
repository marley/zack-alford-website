/**
 * Upcoming / recent shows. Add rows here, or set VITE_BANDSINTOWN_APP_ID
 * in .env to try loading from the Bandsintown API (requires a valid app_id).
 * ISO date (YYYY-MM-DD) for `date`. ticketsUrl optional.
 */
export const manualTourDates = [];
