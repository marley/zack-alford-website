# Vite only exposes env vars prefixed with VITE_.
# Copy to `.env` or `.env.local` and fill in.
# (Previously: REACT_APP_INSTA, REACT_APP_FB, REACT_APP_FIRST, REACT_APP_LAST)

VITE_INSTA=
VITE_FB=
VITE_EMAIL_FIRST=
VITE_EMAIL_LAST=
